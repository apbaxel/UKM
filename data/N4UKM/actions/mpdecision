if [ -f $1 ]; then
	if [[ ! -z $2 ]]; then
	
		if [ $2 == 1 ]; then
			start mpdecision
		elif [ $2 == 0 ]; then
			stop mpdecision
		fi
		
	fi

	if [ "`ps | grep mpdecision`" ]; then
		echo "1"
	else
		echo "0"
	fi
fi;
